package Package;
import java.util.Map;

public class priceMap {


    public priceMap(Map<code, product> products) {
        products.put(code key, product value);
        this.products = products;
    }

    public void add(product whatToAdd) {
        priceMap.put(whatToAdd);
    }
}